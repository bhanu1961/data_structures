#include<stdio.h>
int main()
{
int arr[10],size,x;
scanf("%d",&size);
for(int i=0;i<size;i++)
{
scanf("%d",&arr[i]);
}
for(i=0;i<size;i++)
{
if(x==arr[i])
{
printf("%d is present at location %d",x,i);
break;
}
}
if(c==size)
{
printf("not present");
}
return 0;
}
